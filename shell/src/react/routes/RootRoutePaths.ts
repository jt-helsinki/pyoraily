/*
 *
 * MIT License.
 *
 */
import { RootRoutePaths } from '@src/react/routes/RoutePathNode';

export const ROOT_ROUTE_PATHS: RootRoutePaths = {
  root: '/*',
  login: '/login',
  logout: '/logout',
  authCallback: 'auth/callback',
};
